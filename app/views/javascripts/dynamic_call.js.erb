
var service_centre_slots = new Array()
<% for slot in @service_centre_slots %>
service_centre_slots.push(new Arrays(<%= slot.service_centre_id %>, '<%= h slot.slot_start_time %>',
    '<%= h slot.slot_end_time %>', <%= slot.id %>));
<% end %>


 function serviceSelcted() {
service_id = $("#servicecentre").getValue();
   options =  $("#serviceslot").options;
   options.length = 1;
   service_centre_slots.each(function(slot) {
     if(slot[0] == service_centre_id){
       options[options.length] = new Option(slot[1], slot[2]);

     }
   } );
   if (options.length == 1) {
     $('serviceslot').hide();
   }
   else
   {
     $('serviceslot').show();
   }

 }

document.observe('dom:loaded',function()
{
  $('value').hide();
  $("#servicecentre").observe('change', serviceSelcted);
});
}